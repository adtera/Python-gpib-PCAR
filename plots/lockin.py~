import Gpib,time,string
import random


class Lockin():
	def __init__(self):
		pass #self.device=Gpib.Gpib('lockin')

	def write(self,cmd):
		print cmd#self.device.write(cmd)
	def read(self,bytes):
		return '%d,%d,%d,%d'%(random.random(),random.random(),random.random(),random.random())#self.device.read(bytes)
	def clear(self):
		pass#self.device.clear()
